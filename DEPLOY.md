# Инструкция по деплою на Vercel

## 🚀 Деплой фронтенда на Vercel

### Шаг 1: Подготовка

1. Убедись что все изменения закоммичены и запушены в GitHub:
```bash
git add .
git commit -m "prepare for deployment"
git push origin feature/final
```

### Шаг 2: Деплой через Vercel Dashboard

1. Зайди на [vercel.com](https://vercel.com) и авторизуйся через GitHub
2. Нажми **"Add New Project"**
3. Выбери репозиторий `IlyaKhar/Social_Sh`
4. Выбери ветку `feature/final` (или `main` если там актуальный код)
5. Vercel автоматически определит Next.js проект

### Шаг 3: Настройка переменных окружения

В настройках проекта на Vercel добавь переменную окружения:

**Environment Variable:**
- **Key:** `NEXT_PUBLIC_API_URL`
- **Value:** `https://твой-бэкенд-домен.com` (например, `https://socialsh-api.railway.app`)

**Важно:** 
- Используй `NEXT_PUBLIC_` префикс — это публичная переменная, доступная в браузере
- URL должен быть с `https://` (не `http://`)
- Не добавляй `/api` в конце — он уже есть в коде

### Шаг 4: Деплой

1. Нажми **"Deploy"**
2. Дождись завершения билда (обычно 2-3 минуты)
3. После успешного деплоя получишь URL типа `socialsh-redesign.vercel.app`

---

## 🔧 Деплой бэкенда (Go Fiber)

Бэкенд нужно деплоить отдельно. Варианты:

### Вариант 1: Railway (рекомендую)

1. Зайди на [railway.app](https://railway.app)
2. Создай новый проект
3. Подключи GitHub репозиторий
4. Выбери папку `backend/`
5. Railway автоматически определит Go проект
6. Добавь переменные окружения:
   - `PORT` (Railway сам установит)
   - `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` (для PostgreSQL)
   - `JWT_SECRET`, `REFRESH_SECRET`
   - `BASE_URL` (URL твоего Railway проекта)
   - `TELEGRAM_CHAT_ID` (если используешь)
7. Добавь PostgreSQL базу данных через Railway
8. После деплоя получишь URL типа `socialsh-api.railway.app`

### Вариант 2: Render

1. Зайди на [render.com](https://render.com)
2. Создай новый **Web Service**
3. Подключи GitHub репозиторий
4. Укажи:
   - **Root Directory:** `backend`
   - **Build Command:** `go build -o main ./cmd/api`
   - **Start Command:** `./main`
5. Добавь переменные окружения (как в Railway)
6. Добавь PostgreSQL базу данных через Render

### Вариант 3: Fly.io

1. Установи Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. В папке `backend/` создай `fly.toml`:
```toml
app = "socialsh-api"
primary_region = "fra"

[build]
  builder = "paketobuildpacks/builder:base"

[[services]]
  internal_port = 3001
  protocol = "tcp"
```
3. Деплой: `fly deploy`

---

## 📝 Чеклист после деплоя

- [ ] Фронтенд доступен по URL Vercel
- [ ] Бэкенд доступен по URL (Railway/Render/Fly.io)
- [ ] Переменная `NEXT_PUBLIC_API_URL` указывает на бэкенд
- [ ] База данных подключена и работает
- [ ] Тестовый запрос к API работает (`/api/products`)
- [ ] Загрузка изображений работает
- [ ] Админка доступна и работает

---

## 🔍 Проверка работы

1. Открой фронтенд URL
2. Открой DevTools (F12) → Network
3. Проверь что запросы идут на правильный бэкенд URL
4. Проверь что нет CORS ошибок

---

## ⚠️ Важные моменты

1. **CORS:** Убедись что в бэкенде разрешены запросы с домена Vercel
2. **Изображения:** Загруженные изображения хранятся локально на бэкенде. Для продакшена лучше использовать S3/Cloudinary
3. **База данных:** Используй продакшен базу (не localhost)
4. **Секреты:** Никогда не коммить `.env` файлы с секретами

---

## 🐛 Если что-то не работает

1. Проверь логи на Vercel (Deployments → View Function Logs)
2. Проверь логи бэкенда
3. Проверь переменные окружения
4. Проверь что бэкенд доступен по HTTPS
