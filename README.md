# SOCIAL SH - Интернет-магазин

Полнофункциональный интернет-магазин с админ-панелью.

**Статус**: ✅ Полностью рабочий и стабильный проект

📋 **Полный аудит проекта**: См. [PROJECT_AUDIT.md](./PROJECT_AUDIT.md)

## 🚀 Быстрый старт

### 1. Бэкенд (Go Fiber)

```bash
cd backend

# Установи зависимости
go mod download

# Настрой .env файл (скопируй .env.example если есть)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=maloy
# DB_NAME=socialsh
# JWT_SECRET=your-secret-key
# REFRESH_SECRET=your-refresh-secret
# PORT=3001

# Запусти сервер
go run cmd/api/main.go
```

Бэкенд будет доступен на `http://localhost:3001`

### 2. Фронтенд (Next.js)

```bash
# Установи зависимости
npm install

# Создай .env.local файл
echo "NEXT_PUBLIC_API_URL=http://localhost:3001" > .env.local

# Запусти dev сервер
npm run dev
```

Фронтенд будет доступен на `http://localhost:3000`

## 📋 Что реализовано

### Фронтенд

✅ **Главная страница** - показывает новые товары и скидки  
✅ **Магазин** - все товары, новые поступления, сезонные скидки  
✅ **Галерея** - фотографии с фильтрацией по категориям  
✅ **Информационные страницы** - оплата, доставка, возврат, контакты  
✅ **Личный кабинет** - профиль и заказы (требует авторизации)  
✅ **Админ-панель** - полный CRUD для товаров, галереи и страниц

### Компоненты

- `ProductCard` - карточка товара
- `ProductGrid` - сетка товаров
- `GalleryGrid` - сетка галереи
- API утилиты (`lib/api.ts`) - все запросы к бэкенду

### Админ-панель

- Авторизация (admin@socialsh.ru / admin123)
- Управление товарами (создание, редактирование, удаление)
- Управление галереей (создание, редактирование, удаление)
- Редактирование статических страниц

## 🔧 API

Все эндпоинты документированы в Swagger UI:
```
http://localhost:3001/swagger
```

### Основные эндпоинты

**Публичные:**
- `GET /api/products` - список товаров
- `GET /api/products/:slug` - один товар
- `GET /api/gallery` - галерея
- `GET /api/pages/:slug` - статические страницы

**Авторизация:**
- `POST /api/auth/sign-in` - вход
- `POST /api/auth/sign-up` - регистрация
- `GET /api/auth/is-admin` - проверка админки

**Личный кабинет:**
- `GET /api/account/me` - профиль
- `GET /api/account/orders` - заказы
- `PATCH /api/account/me` - обновить профиль

**Админка:**
- `GET /api/admin/products` - список товаров
- `POST /api/admin/products` - создать товар
- `PATCH /api/admin/products/:id` - обновить товар
- `DELETE /api/admin/products/:id` - удалить товар
- Аналогично для `/api/admin/gallery` и `/api/admin/pages`

## 🖼️ Изображения

Пока используются placeholder изображения из `picsum.photos`. Для реальных изображений:

1. Загрузи файлы на сервер или используй внешний хостинг
2. В админке укажи полный URL изображения
3. Для товаров можно указать несколько URL через запятую

## 📝 База данных

PostgreSQL схема в `sql.sql`, тестовые данные в `sql_seed.sql`.

```bash
# Создать БД и применить схему
psql -U maloy -d socialsh -f sql.sql

# Заполнить тестовыми данными
psql -U maloy -d socialsh -f sql_seed.sql
```

## 🎨 Дизайн

- Черно-белая цветовая схема
- Минималистичный дизайн
- Адаптивная верстка
- Боковая навигация

## 📦 Структура проекта

```
.
├── app/              # Next.js страницы
│   ├── shop/         # Магазин
│   ├── gallery/      # Галерея
│   ├── admin/        # Админ-панель
│   └── account/      # Личный кабинет
├── components/       # React компоненты
├── lib/              # Утилиты (API)
├── backend/          # Go бэкенд
│   ├── cmd/api/      # Точка входа
│   ├── internal/     # Внутренние пакеты
│   └── docs/         # Swagger документация
└── sql.sql           # Схема БД
```

## 🔐 Безопасность

- JWT токены для авторизации
- Роли пользователей (user/admin)
- Защищённые роуты через middleware
- Хеширование паролей (bcrypt)

## 🐛 Отладка

**Проблемы с подключением к БД:**
- Проверь что PostgreSQL запущен
- Проверь настройки в `.env`
- Проверь что база `socialsh` существует

**Проблемы с API:**
- Проверь что бэкенд запущен на порту 3001
- Проверь CORS настройки
- Проверь Swagger UI для тестирования эндпоинтов

**Проблемы с фронтом:**
- Проверь что `NEXT_PUBLIC_API_URL` установлен в `.env.local`
- Перезапусти dev сервер после изменения `.env.local`
- Проверь консоль браузера на ошибки

## 📚 Документация

- `SWAGGER.md` - инструкция по использованию Swagger UI
- `md/backend.md` - подробная документация бэкенда
- `md/frontend.md` - документация фронтенда (если есть)

## 🚧 TODO

- [ ] Загрузка изображений через форму
- [ ] Пагинация для товаров
- [ ] Поиск товаров
- [ ] Корзина и оформление заказа
- [ ] Email уведомления
- [ ] Аналитика
